const from = document.querySelector("form") as HTMLFormElement;
type User = {
    id: number;
    name: string;
    age: string;

};
 let users: User[] = [];

 function reducerUsers(users: User[]) {
    users.forEach ((item. User) => {
        const { id, name, age} = item 
    })
 }
